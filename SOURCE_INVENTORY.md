# سجل مصادر إعادة بناء ميتا - بوت الجراح

## المصادر التي تم فحصها ودمجها

| المصدر | الحالة | ما تم استخدامه |
| --- | --- | --- |
| `aljarrahmahmoud15-tech/-meta-bot-2026-` | متاح عبر GitHub | النسخة الأساسية، نظام الطلبات والمحافظ والتسويات، الهدايا، الاشتراكات، لوحة الإدارة، الاختبارات، وملفات البيئة والنشر |
| `aljarrahmahmoud15-tech/whatsapserver` | متاح عبر GitHub | تحسينات Chromium وملكية `/app/data` في Docker، إعدادات تشغيل WhatsApp، ومراجعة توافق Render |
| `https://bot.wasselni-biz.com/` | متاح للقراءة العامة | بنية لوحة الإدارة الزجاجية، أقسام الحالة، الهدايا، المحافظ، الطلبات، التسويات، الكباتن، المستخدمين، والموظفين |
| Render service `srv-dakjt87qi5pc73ber000` | متاح عبر جلسة المتصفح | مراجعة سجل النشر، والتحقق من build ناجح، وظهور fallback التخزين، وحالة Chromium |

## مصادر لم تكن قابلة للسحب

لم يكن هناك موصل Railway أو معرف/صلاحية لمشروعي `friendly-analysis` و`handsome-enjoyment` في جلسة التنفيذ، لذلك لم يتم اختلاق أو نسخ ملفات منهما. كما لم يتم تغيير `whatsapserver-2` أو أي خدمة أخرى خارج المستودع الأساسي.

## قرارات الدمج

أُبقي منطق المشروع الأساسي بدل نسخ `server.js` كاملًا من مصدر آخر حتى لا تُفقد جداول الطلبات والمحافظ والتسويات والهدايا. تم دمج Docker الأكثر اكتمالًا من مصدر `whatsapserver`، وتوحيد التخزين على `/app/data`، وإضافة fallback قابل للتشخيص عند غياب القرص. الأسرار `ADMIN_TOKEN` و`JWT_SECRET` لا توجد في Git؛ تُضبط في بيئة Render فقط.
